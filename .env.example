# Optional: automatic receipt reading. Manual entry works without this.
OPENAI_API_KEY=
RECEIPT_MODEL=gpt-4.1-mini
# SQLite file. Use an absolute path to a persistent volume when deploying.
DATABASE_PATH=./data/groceries.sqlite
